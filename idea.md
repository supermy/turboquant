
谷歌TurboQuant：使您的人工智能运行成本低六倍的算法

技术性抛开：标准量化在笛卡尔空间的向量上操作——沿X、Y、Z轴的坐标。每个数据块的边界取决于数据本身，迫使算法存储归一化常数，以便它以后知道如何解释压缩值。TurboQuant的PolarQuant组件将向量转换为极坐标：半径（大小）和一组角度（方向）。高维向量中的角分布是已知的、集中的和可预测的——这意味着不需要归一化步骤。坐标网格的边界已经固定。设计上没有开销。


https://arxiv.org/html/2504.19874v1#S3
https://arxiv.org/html/2603.29078v1
https://www.promptinjection.net/p/google-turboquant-the-algorithm-that-makes-ai-llm-faster

向量量化是一个植根于Shannon源编码理论的问题，旨在量化高维欧几里得向量，同时尽量减少其几何结构的失真。我们建议使用TurboQuant来解决均方误差（MSE）和内积失真，克服了无法实现最佳失真率的现有方法的局限性。我们的数据忘乎所以算法适用于在线应用程序，在所有位元宽度和维度上实现接近最佳的失真率（在小常数系数内）。TurboQuant通过随机旋转输入向量，在坐标上诱导集中的Beta分布，并利用高维不同坐标的近独立属性，简单地为每个坐标应用最佳标量量化器来实现这一目标。认识到MSE最优量化器在内积估计中引入了偏差，我们提出了一种两阶段的方法：应用MSE量化器，然后对残差进行1位量化JL（QJL）变换，从而产生无偏不倚的内积量化器。我们还提供了任何向量量化器的最佳可实现失真率的信息理论下限的正式证明，证明TurboQuant与这些界限密切匹配，只有一个小常数（

≈
2.7
\approx 2.7
）因素。实验结果验证了我们的理论发现，表明对于KV缓存量化，我们以每通道3.5位实现绝对质量中和以每通道2.5位实现边际质量退化。此外，在最近邻居搜索任务中，我们的方法在召回中优于现有的产品量化技术，同时将索引时间几乎减少到零。

我们介绍PolarQuant，一种大型语言模型（LLM）的训练后权重量化方法，它利用神经网络权重的分布结构来实现近乎无损的压缩。PolarQuant分三个阶段运行：（1）块状归一化到单位超球体，（2）沃尔什-哈达达德旋转将坐标转换为近似高斯随机变量，以及（3）量化与高斯分布匹配的重心。我们的消融表明，仅Hadamard旋转就占了质量改进的98%，将Qwen3.5-9B的困惑从6.90（absmax Q5）减少到6.40（
Δ
=
+
0.03
\Delta=+0.03
来自FP16），使其几乎无损，没有任何校准数据。此外，PolarQuant是下游INT4量化器的有效预处理步骤：PolarQuant Q5由torchao INT4去量化和重新量化，实现了直接absmax INT4的6.68的困惑度，而6.56.68，同时在6.5 GB VRAM下保持43.1 tok/s的吞吐量。代码和模型是公开的。




